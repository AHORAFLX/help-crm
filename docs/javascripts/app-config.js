// Nombre de la subcarpeta dentro de la app donde está publicada esta documentación (wwwroot/crm/docs).
// Se usa para que "Ejecutar proceso" navegue a la raíz real de la app y no a la ruta anidada de la ayuda.
const FH_DOCS_SUBFOLDER = 'crm';
